// Write a function that will check two given characters are the same case

// function checkCharacterCasing('a', 'c') => true

function checkCharacterCasing(x, y){

  if(x == x.toUpperCase() && y == y.toUpperCase()){
    return true;
  } else if( x == x.toLowerCase() && y == y.toLowerCase()){
    return true
  }
  return false;
}

for (let index = 0; index < array.length; index++) {
  const element = array[index];
  
}

console.log(checkCharacterCasing('a', 'D'));